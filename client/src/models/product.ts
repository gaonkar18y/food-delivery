export interface Product {
    name: string;
    description: string;
    price: number;
    id: number;
    imageUrl: string;
}

export interface ProductCategory{
    name: string;
    description: string;
    id: number;
}